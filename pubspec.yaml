name: special_data_flutter
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_local_notifications: ^14.1.1
  parse_server_sdk_flutter: ^4.0.0
  flutter_secure_storage: ^8.0.0
  encrypt: ^5.0.1
  local_auth: ^2.1.6
  camera: ^0.10.4
  path_provider: ^2.0.14
  path: ^1.8.2
  flutter_riverpod: ^2.2.0
  get_it: ^7.3.0
  mockito: ^5.4.0

flutter:
  uses-material-design: true
